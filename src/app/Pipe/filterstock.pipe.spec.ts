import { FilterstockPipe } from './filterstock.pipe';

describe('FilterstockPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterstockPipe();
    expect(pipe).toBeTruthy();
  });
});
